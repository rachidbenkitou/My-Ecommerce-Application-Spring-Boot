cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    region:
      static: eu-west-1
    stack:
      auto: false

application:
  bucket:
    name: ecommerceservice-file


jwt:
  auth:
    converter:
      resource-id: ecommerceservice-service-client
      principle-attribute: preferred_username

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/ecommerce-service-realm
          jwk-set-uri: ${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

  mail:
    host: smtp.gmail.com
    port: 465
    username: rachidbenkitou2021@gmail.com
    password: kxmd uimm jntc qxqq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    url: jdbc:mysql://localhost/ecommerce?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /api
  port: 9090


# My Custom Params
myKeycloak:
  users-endpoint: http://localhost:8080/admin/realms/ecommerce-service-realm/users
  token-endpoint: http://localhost:8080/realms/ecommerce-service-realm/protocol/openid-connect/token
  logout-endpoint: http://localhost:8080/realms/ecommerce-service-realm/protocol/openid-connect/logout
  roles-endpoint: http://localhost:8080/admin/realms/ecommerce-service-realm/roles

  client:
    default-role-id: 2a0e9424-ed1b-4ec6-8138-daec9c1e9735
    default-role-name: client